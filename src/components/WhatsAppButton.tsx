import { useState, type MouseEvent, type ReactNode } from "react";
import WhatsAppIcon from "./WhatsAppIcon";
import { openWhatsAppGroup, WHATSAPP_GROUP_URL } from "../lib/whatsapp";
import { trackWhatsAppLead } from "../lib/meta";
type Props={className:string;label:ReactNode;iconClassName?:string};
export default function WhatsAppButton({className,label,iconClassName="w-7 h-7"}:Props){const[stuck,setStuck]=useState(false);const handleClick=(event:MouseEvent<HTMLAnchorElement>)=>{if(event.metaKey||event.ctrlKey||event.shiftKey||event.button!==0)return;event.preventDefault();trackWhatsAppLead();openWhatsAppGroup(()=>setStuck(true));};return <>{<a href={WHATSAPP_GROUP_URL} onClick={handleClick} target="_blank" rel="noopener noreferrer" className={className}><WhatsAppIcon className={iconClassName}/><span>{label}</span></a>}{stuck&&<div className="fixed inset-0 z-[100] flex items-center justify-center p-4 bg-black/85 backdrop-blur-sm" role="dialog" aria-modal="true" aria-label="Como abrir o grupo do WhatsApp"><div className="glass-card w-full max-w-sm rounded-2xl p-6 text-center"><div className="text-4xl mb-3">📲</div><h3 className="font-heading text-xl font-black text-white mb-2">Falta só um passo!</h3><p className="text-sm text-gray-300 mb-4 leading-relaxed">Você está no navegador interno do app. Toque no menu <strong className="text-white">⋮</strong> (canto superior direito) e escolha <strong className="text-white">“Abrir no navegador”</strong> para entrar no grupo automaticamente.</p><div className="flex flex-col gap-2"><a href={WHATSAPP_GROUP_URL} onClick={handleClick} target="_blank" rel="noopener noreferrer" className="btn-whatsapp-glow flex items-center justify-center gap-2 bg-linear-to-r from-[#25D366] to-[#1DA851] text-white font-extrabold px-6 py-3.5 rounded-xl text-base"><WhatsAppIcon className="w-5 h-5"/><span>Tentar novamente</span></a><button type="button" onClick={()=>setStuck(false)} className="text-sm font-semibold text-gray-400 hover:text-white transition-colors py-2">Fechar</button></div></div></div>}</>}
